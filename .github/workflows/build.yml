name: Go Build

permissions:
  "contents": "write"

on:
  push:
    branches: ["master", "main"]
  pull_request:
    branches: ["master", "main"]

jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        include:
          - os: ubuntu-latest
            output: Discord-Message-Cleaner
          - os: macos-latest
            output: Discord-Message-Cleaner
          - os: windows-latest
            output: Discord-Message-Cleaner.exe
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.3"
      - name: Build
        run: go build -o ${{ matrix.output }} .
      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: Discord-Message-Cleaner-${{ matrix.os }}
          path: ${{ matrix.output }}
  create-release:
    needs: [build]
    runs-on: ubuntu-latest
    if: (github.event_name == 'push' && github.ref == 'refs/heads/main')

    steps:
      - uses: actions/checkout@v6
        name: Checkout Code

      # Windows
      - uses: actions/download-artifact@v8
        name: Download artifacts
        with:
          path: release-artifacts/
          merge-multiple: true

      - name: Rename Windows Binary
        run: mv release-artifacts/Discord-Message-Cleaner.exe release-artifacts/Discord-Message-Cleaner-Windows.exe

      - name: Rename Linux Binary
        run: mv release-artifacts/Discord-Message-Cleaner release-artifacts/Discord-Message-Cleaner-Linux

      - name: Rename Mac Binary
        run: mv release-artifacts/Discord-Message-Cleaner release-artifacts/Discord-Message-Cleaner-Mac

      - name: Create release
        uses: ncipollo/release-action@v1.21.0
        with:
          replacesArtifacts: true
          allowUpdates: true
          artifactErrorsFailBuild: true
          artifacts: "release-artifacts/*"
          body: ${{ github.event.head_commit.message }}
          prerelease: true
          name: Nightly Release
          tag: nightly-build

      - name: Update nightly-build tag
        run: |
          git tag -f nightly-build
          git push -f origin nightly-build
        shell: bash
